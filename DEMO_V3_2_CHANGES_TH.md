# สิ่งที่แก้ไข — ERP DEMO v3.2

Package 1.3.0 · 5 กันยายน 2569

แก้ต่อจาก Integrated v3.1 โดยคง Local DEMO ชื่อบริษัท โลโก้ และสาขาเดิม ไม่ได้เปิด Firebase หรือ deploy ระบบ

## ผลแก้ปัญหา 6 ข้อ

| ข้อ | สิ่งที่แก้ | ผลทดสอบหลังแก้ |
|---|---|---|
| D01 กู้ Invoice แล้วสต๊อกติดลบ | ตรวจสต๊อก ยอดจอง จำนวนส่งค้าง และเลขเอกสารก่อนกู้ ใช้ SO ของเอกสารที่กู้เป็นตัวอ้างอิง | สต๊อกคง 0; ปฏิเสธกู้ Invoice ที่ทำให้เหลือ −10 และเก็บรายการในถังขยะไว้ |
| D02 กู้ใบเสร็จแล้วรับเงินเกิน | ตรวจบิล ลูกค้า สาขา และยอดค้างก่อนกู้ | บิล 1,070 รับเงินรวมยัง 1,070; ไม่เพิ่มเป็น 2,140 |
| D03 ต้นทางกับฉบับพิมพ์ต่างกัน | ล็อกการแก้ต้นทาง Invoice/Receipt เมื่อมีฉบับพิมพ์ที่บันทึกและยังใช้งานอยู่ ตรวจซ้ำตอนบันทึก | ทดลองเปลี่ยนยอด 1,070 เป็น 2,140 ถูกปฏิเสธ; ทั้งสองฉบับยัง 1,070 |
| D04 Billing ไม่ตามเงินรับช่องทางอื่น | คำนวณสถานะและยอดค้างจาก Payment และใบเสร็จตรง พร้อมเก็บยอดรับก่อนสร้างใบวางบิล | Invoice รับครบแล้ว Billing เป็น paid; เงินที่รับก่อนวางบิลไม่ถูกหักซ้ำ |
| D05 ต้นทุนผลิตไม่ไปกับ Invoice | จัดสรรต้นทุนงานผลิตที่เชื่อม SO ตามจำนวนส่ง เก็บที่มาและยอดจัดสรรในแต่ละบรรทัด | ต้นทุน 600 / ขาย 1,000 → Invoice และ Dashboard แสดงกำไร 400 ในชุดทดสอบ |
| D06 รับสินค้าบันทึกได้ครึ่งเดียว | เตรียม GR, Stock Movement, PO status และ Audit แล้วบันทึกเป็นชุดเดียว พร้อม rollback | จำลองล้มเหลวแต่ละจุดแล้วข้อมูลคงเดิม; ลองใหม่ได้โดยไม่ซ้ำ |

## รายละเอียดที่ผู้ทดลองควรรู้

### กู้ข้อมูล

รายการที่กู้ไม่ได้จะยังอยู่ใน Recycle Bin พร้อมข้อความอธิบาย ไม่ถูกลบทิ้ง การกู้คืนที่เงื่อนไขครบยังทำงานได้ตามเดิม และการย้ายออกจากถังขยะ/เพิ่มเอกสาร/เพิ่ม Audit จะบันทึกพร้อมกัน

ฉบับพิมพ์เก่าที่กู้ไม่ตรงกับต้นทางจะถูกปฏิเสธ ให้เปิดต้นทางแล้วสร้างฉบับพิมพ์ใหม่ ข้อมูลที่เคยผิดก่อนอัปเดตไม่ได้ถูกแก้ตัวเลขย้อนหลังเงียบ ๆ

เพิ่มการตรวจสต๊อกในฟังก์ชันบันทึก Invoice โดยตรงด้วย เพื่อไม่ให้ความถูกต้องพึ่งเฉพาะลำดับการติดตั้งตัวครอบฟังก์ชันตอนเปิดหน้า

### เอกสารที่บันทึกฉบับพิมพ์แล้ว

ใบเสนอราคา/Invoice/Receipt ที่ยังเป็นร่างให้ตรวจข้อมูลก่อนบันทึกฉบับพิมพ์ สำหรับ Invoice และ Receipt ที่มีฉบับพิมพ์บันทึกแล้ว รุ่น DEMO นี้ล็อกการแก้ต้นทาง การดู Preview อย่างเดียวไม่ใช่การบันทึกฉบับพิมพ์

รอบนี้ยังไม่ได้เพิ่มระบบ revision, ใบลดหนี้ หรือขั้นตอนแก้เอกสารออกจริงครบวงจร จึงไม่ควรใช้ DEMO ออกเอกสารธุรกิจจริง หากข้อมูลเดิมมีต้นทางกับฉบับพิมพ์ต่างกันอยู่แล้ว ต้องตรวจคู่เอกสารนั้นก่อนใช้ต่อ

### วางบิลและรับเงิน

- Payment ที่อ้าง Billing จัดสรรให้ Billing นั้น
- ใบเสร็จตรงที่ไม่ได้ผูก Payment จัดสรรให้ใบวางบิลตามลำดับวันที่ และไม่ใช้เงินที่รับก่อนสร้าง Billing ซ้ำอีก
- เมื่อย้อนรับเงิน/ลบใบเสร็จที่อนุญาต ยอดค้างและสถานะจะคำนวณใหม่
- ตารางใบวางบิลแสดงยอดวางบิลเดิมและยอดค้างปัจจุบัน
- เก็บสถานะเอกสารวางบิลแยกจากสถานะรับเงิน เพื่อไม่ให้ข้อมูลขั้นตอนเอกสารถูกแทนด้วยคำว่า paid อย่างเดียว

### ต้นทุนและกำไร

สำหรับงานผลิตที่เชื่อม SO ระบบจับคู่บรรทัดด้วยรหัสบรรทัด SO ก่อน หากเป็นข้อมูลเก่าใช้รหัสสินค้าได้เมื่อจับคู่ไม่กำกวม จัดสรรต้นทุนที่บันทึกในงานผลิตตามจำนวนส่ง โดยหักต้นทุนที่จัดสรรไปแล้ว และรักษาผลรวมเมื่อปัดเศษสตางค์

เก็บ `costAllocation` ใน Invoice เพื่อบอกฐานต้นทุน เลขงานผลิต ยอดต้นทุนรวม และยอดที่จัดสรรก่อนหน้า ต้นทุนที่มาจาก SO จะแสดงแบบอ่านอย่างเดียวในฟอร์มส่งสินค้า ให้แก้ข้อมูลต้นทางที่เกี่ยวข้อง

หากมีเพียงต้นทุนมาตรฐาน งานผสม Stock/ผลิต หรือไม่มีหลักฐานต้นทุนเพียงพอ ระบบขึ้น “กำไรเบื้องต้น” ในฟอร์มและแจ้งเตือนบน Dashboard/Analytics เมื่อมีเอกสารกลุ่มนี้ในช่วงที่ดู ไม่ตีความช่องต้นทุนว่างว่าเป็นต้นทุนจริง 0 บาท

นี่เป็นการจัดสรรต้นทุนสำหรับ DEMO ยังไม่ใช่ระบบบัญชีต้นทุนแบบ lot/FIFO/average cost เต็มรูปแบบ และต้นทุนที่เคยขาดใน Invoice รุ่นเก่าไม่ได้ถูกแก้ย้อนหลังอัตโนมัติ ต้องตรวจข้อมูลต้นทางก่อน

### การบันทึกล้มเหลว

รายการรับสินค้าจะบันทึกข้อมูลที่เกี่ยวข้องครบชุดก่อนแจ้งสำเร็จ เมื่อเขียนล้มเหลวจะย้อนเฉพาะ key ที่เปลี่ยนแล้ว หาก rollback เองล้มเหลว ระบบแจ้งให้กู้ Backup แทนการแจ้งสำเร็จ

ขอบเขต transaction นี้เป็นการทำงานใน Browser เดียว ไม่ใช่การป้องกันหลายผู้ใช้หรือหลายแท็บเขียนพร้อมกัน

## ไฟล์หลักที่แก้

- `erp-integrity.js`: สถานะ Billing, ล็อกเอกสาร, จัดสรรต้นทุน และ transaction/rollback
- `erp-production-core.js`: ตรวจและกู้เอกสาร, การรับสินค้าครบชุด, ตรวจสต๊อกโดยระบุ SO ชัดเจน
- `erp-order-flow-v3.js`: อ่านสถานะ Billing ที่คำนวณแล้ว ยอดรับก่อนวางบิล และยอดค้างในตาราง
- `app.js`: ตรวจตอนบันทึกจริง ล็อกเอกสาร คำนวณต้นทุนส่งสินค้า และข้อความเตือนกำไร
- `index.html`: ตำแหน่งข้อความต้นทุน/กำไรเบื้องต้น
- `tests/`: เพิ่มกรณีที่เคยพบปัญหาและกรณีต่อเนื่อง

## การตรวจรับ

ผลล่าสุดอยู่ใน `VALIDATION_TH.md`, `test-results.txt` และ `build-results.txt` ใช้ `npm test` และ `npm run build` ตรวจซ้ำได้

ผลทดสอบครอบคลุมข้อมูลและ DOM จำลอง ยังต้องตรวจ Preview/PDF ภาษาไทยและทดลองบน Browser ของหัวหน้าก่อนส่งสาธิต โดยใช้ข้อมูลสมมติและสำรองข้อมูลไว้ก่อน
