# รายงานตรวจโค้ดและการแสดงผล ERP DEMO v3.2.1

วันที่ตรวจ: 6 กันยายน 2569 · package 1.3.1 · ฐาน: Integrated DEMO v3.2 ที่แก้ปัญหาธุรกิจ 6 ข้อแล้ว

## ข้อสรุปที่ต้องแยกกัน

1. พบโค้ด JavaScript ครอบฟังก์ชันซ้ำที่บล็อกการบันทึกของตัวเองได้จริง และแก้แล้ว
2. พบ CSS ปรับทับกันหลายชั้นจริง แต่ยังไม่มีหลักฐานว่าเป็นต้นเหตุที่ทั้งหน้าไม่มีรูปแบบตามภาพ
3. ภาพที่ส่งสอดคล้องกับ CSS และโลโก้โหลดไม่สำเร็จ: ในต้นฉบับ style.css บรรทัด 22 กำหนดขนาดไอคอน, บรรทัด 123 ซ่อนกล่องรายละเอียด, บรรทัด 142 กำหนดโลโก้และหน้าตาเว็บ เมื่อรูปแบบไม่ถูกใช้ องค์ประกอบเหล่านี้จะปรากฏผิดลักษณะพร้อมกัน
4. ยังไม่ได้รับ URL GitHub Pages หรือ repository จึงยังตรวจ HTTP, ไฟล์ที่เผยแพร่จริง, branch, โฟลเดอร์เผยแพร่ และแคชของเว็บจริงไม่ได้ การแก้ใน ZIP ไม่ได้เปลี่ยนเว็บไซต์ที่เผยแพร่ให้อัตโนมัติ

## ขอบเขตการตรวจอย่างตรงไปตรงมา

ตรวจเชิงโครงสร้างครบ 29 ไฟล์ HTML/CSS/JavaScript/config รวม 22,764 บรรทัดของฐานก่อนแก้ ด้วยตัวแยกไวยากรณ์ JavaScript, CSS และ HTML และอ่านรายละเอียดไล่การเรียกในจุดเริ่มระบบ การประกาศฟังก์ชันบน window, ตัวครอบฟังก์ชัน, การโหลดทรัพยากร และการพิมพ์

นี่ไม่ใช่การรับรองว่าได้ตรวจตรรกะธุรกิจทุกบรรทัดด้วยมือหรือทดสอบทุกเส้นทาง ทุกข้อมูล และทุกขนาดหน้าจอ ไม่มีการตรวจหน้าตาด้วยเบราว์เซอร์จริงหรือการจัดหน้ากระดาษ PDF ในรอบนี้

รายชื่อและจำนวนบรรทัดทุกไฟล์อยู่ใน docs/code-review/FILE_REVIEW_MAP.csv; รายงานโครงสร้างเต็มอยู่ใน structure-before.json; รายการ CSS ซ้ำพร้อมตำแหน่งอยู่ใน CSS_OVERLAPS.csv เลขบรรทัดในหลักฐานเหล่านี้อ้างฐานก่อนแก้

## ข้อผิดพลาดที่แก้ในรอบนี้

| รหัส | จุดพบ | ก่อนแก้ / หลักฐาน | หลังแก้ |
|---|---|---|---|
| W01 | local-demo-health.js:27 | ตัวติดตั้ง guard ตรวจ marker เฉพาะฟังก์ชันชั้นนอก เมื่อ Trial/โมดูลอื่นครอบ guard เดิม การตรวจรอบถัดไปติด guard ซ้ำ ทั้งสองใช้ inflight เดียวกัน ทำให้กดครั้งเดียวแต่บันทึก 0 รายการและแจ้งว่ากำลังทำงาน | จดจำการติดตั้งต่อชื่อฟังก์ชัน ไม่ติด guard เดิมซ้ำเมื่อถูกครอบภายหลัง ทดสอบว่าบันทึกครั้งแรกได้ กดซ้ำทันทีถูกกัน และบันทึกครั้งถัดไปหลังจบได้ |
| W02 | app.js:7544,8003; boot-status.js | ComformAppReady ถูกตั้ง true ตั้งแต่ expose handler ก่อน boot UI เสร็จ และไม่มีการส่ง comform-app-ready แม้มีผู้รับ event | ตั้ง true และส่ง event หลัง boot สำเร็จเท่านั้น หากผิดพลาดตั้ง false และส่ง comform-app-failed; ป้องกัน boot ที่สำเร็จแล้วรันซ้ำ |
| W03 | delivery-tax-document.js:1393; receipt-document.js:1528 | หน้าพิมพ์เรียก CSS ชื่อเดิมจาก window.location.href แต่ dist ไม่มีไฟล์สองชื่อนั้น | ใช้ import.meta.url ให้ Vite สร้างและอ้างไฟล์ CSS ที่มี hash; ตรวจ HTML หน้าพิมพ์จาก compiled bundle แล้วมีไฟล์ปลายทางทั้ง 3 แบบ |
| W04 | quotation-document.js:465; delivery-tax-document.js:1388; receipt-document.js:1523 | เปิด popup ด้วย noopener,noreferrer แล้วคาดหวัง window handle สำหรับ document.write; ตามพฤติกรรม API noopener คืน null จึงอาจเปิดแท็บว่างและแจ้งว่าถูกบล็อก | เปิด about:blank รับ handle แล้วตั้ง opener=null ก่อนเขียนเอกสาร ทดสอบสัญญา API ด้วย popup จำลองทั้งสามแบบ ยังต้องตรวจการอนุญาต popup บนเบราว์เซอร์ผู้ใช้ |
| W05 | index.html; boot-status.js | ตัวตรวจโหลดเดิมรวมใน bundle เดียวกับระบบ ถ้า bundle หาย ตัวตรวจเองก็ไม่ทำงาน; ข้อความเดิมชี้ไป Vercel Environment Variables ทั้งที่เป็น Local DEMO | เพิ่ม inline watchdog อิสระจาก bundle ตรวจ error ของ CSS/JS/รูปภาพ และแจ้ง main boot ไม่สำเร็จ เพิ่มข้อความกับลิงก์ตรวจไฟล์ที่ตรงกับ DEMO |
| W06 | local-demo-health.js | สรุปว่าพร้อมสาธิตจากจำนวน runtime error/stock โดยไม่ได้รวม appReady ในความเสี่ยง | รวม appReady และเปลี่ยนข้อความเป็นผลของรายการตรวจอัตโนมัติ ไม่รับรองความพร้อมทั้งหมด |

อ้างอิงพฤติกรรม window.open: [MDN Window.open](https://developer.mozilla.org/en-US/docs/Web/API/Window/open#noopener) — noopener คืน null และ noreferrer เปิด noopener ด้วย

## ผลตรวจโค้ดทับซ้อน

- ไม่พบ id ซ้ำใน HTML ต้นฉบับ
- ไม่พบ URL ทรัพยากรซ้ำในรายการ script/link/img ต้นฉบับ
- ไม่พบไฟล์ที่ขาดจากการอ้างแบบคงที่ใน HTML ต้นฉบับ แต่การอ้าง CSS หน้าพิมพ์แบบ dynamic เดิมมีข้อผิดพลาด W03
- ไม่พบการประกาศชื่อฟังก์ชันซ้ำในขอบเขตเดียวกันจาก AST; ชื่อ esc, money, init ฯลฯ ที่เหมือนกันข้ามโมดูล/closure ไม่ใช่ global ชื่อเดียวกัน
- พบ selector CSS ซ้ำในไฟล์เดียวกันและเงื่อนไข @media/@rule เดียวกัน 504 กลุ่ม ตัวเลขนี้เป็นจำนวนกลุ่มซ้ำ ไม่ใช่จำนวนบั๊ก บางกลุ่มเพิ่มคนละ property บางกลุ่มปรับค่าเดิม
- ตัวอย่าง .dtd-doc-items-table th และ .rcp-doc-items-table th ประกาศอย่างละ 9 ครั้ง; .nav-item svg ปรับจาก 15px เป็น 18px การเปลี่ยนขนาดลักษณะนี้อธิบายไอคอนระดับ 15–18px แต่ไม่อธิบายการขยายเต็มหน้าพร้อมโลโก้หาย
- window.go มี fallback → core → production → order-flow; saveQuote/saveInvoice/saveReceipt มี Trial/Audit/Stock/Single-flight ครอบต่อกัน ต้องรักษาลำดับและการติดตั้งเพียงครั้งเดียว W01 คือข้อผิดพลาดที่พิสูจน์ได้จากโครงสร้างนี้

ยังคง CSS เดิมไว้ในรอบนี้ เพราะการลบ selector ที่ดูซ้ำทั้งหมดอาจเปลี่ยน cascade, responsive และเอกสารพิมพ์ ข้อเสนอรอบถัดไปคือแยก base/theme/layout/print แล้วตรวจภาพก่อน–หลังแต่ละหน้าก่อนรวมกฎ ให้ทำหลังแก้การโหลดไฟล์บนเว็บจริงได้แล้ว

## ส่วนช่วยตรวจ GitHub Pages ที่เพิ่ม

- inline watchdog ใน index.html: เมื่อไฟล์ไม่ครบจะแสดงชื่อไฟล์และเลขรุ่น DEMO 3.2.1 แม้ JavaScript หลักไม่โหลด
- จำกัดขนาด SVG เมนูและกำหนดขนาดโลโก้เริ่มต้น รวมถึงซ่อน modal ที่ยังไม่เปิด เพื่อไม่ให้ไอคอนขยายเต็มหน้าจอระหว่าง CSS ขาด ส่วนนี้ไม่ทดแทน CSS หลัก
- deployment-check.html: หน้าอิสระตรวจ HTTP, MIME type และ SHA-256 ของไฟล์เทียบชุด build ตรวจพบไฟล์เก่า/ปนรุ่นได้ ใช้ผ่าน HTTPS หรือ localhost
- scripts/build-deployment-check.mjs: สร้างหน้าตรวจและ .nojekyll อัตโนมัติหลัง npm run build ไม่ต้องแก้ชื่อ hash ด้วยมือ

## ผลทดสอบ

- npm test: 49 กรณีเดิมผ่านทั้งหมดหลังแก้
- npm run test:deployment: 5 กรณีใหม่ผ่านทั้งหมด — compiled ESM ที่ /demo-repo/, บันทึกหลังโมดูลติดตั้งล่าช้า, หน้าพิมพ์ทั้ง 3 แบบ, bundle/CSS หาย, boot ล้มเหลว, guard ซ้อน (บางกรณีมีหลาย assertions)
- npm run build: ผ่าน มีคำเตือนขนาด JavaScript หลักเกิน 500KB เป็นเรื่องประสิทธิภาพ ไม่ใช่ build ล้มเหลว
- HTTP จำลองที่ /test-repository/: HTML, JavaScript, CSS รวมไฟล์พิมพ์, โลโก้ และหน้าตรวจ 8 ไฟล์ตอบ 200 พร้อม MIME และเนื้อหาตรงไฟล์ต้นทาง
- ทดสอบ ESM ด้วย Node VM และ DOM จำลอง ไม่ใช่เบราว์เซอร์จริง; ไม่ได้ยืนยัน CDN สร้าง PDF หรือหน้าตา PDF; ไม่ได้ตรวจ GitHub Pages จริง

ผลดิบ: test-results.txt, deployment-test-results.txt, deployment-http-results.json, build-results.txt

## สิ่งที่ต้องใช้เพื่อตรวจปัญหาหน้าเว็บต่อ

ส่ง URL GitHub Pages ที่เปิดภาพนี้ หรือ URL repository ถ้าเข้าถึงได้ หลังอัปโหลด ZIP ใหม่ให้เปิด deployment-check.html จากโฟลเดอร์เดียวกับหน้า ERP แล้วส่งผลตรวจ หากหน้าตรวจเป็น 404 ต้องตรวจว่า deployment ใช้ branch/folder ใดก่อน ไม่ควรสรุปว่าปัญหามาจากแคชอย่างเดียว

ระบบยังเป็น Local DEMO และไม่มีการเปลี่ยนไปใช้ฐานข้อมูลร่วม/ระบบสมาชิกจริงในรอบนี้
